import { ThemeSwitcherDirective } from './theme-switcher.directive';

describe('ThemeSwitcherDirective', () => {
  it('should create an instance', () => {
    const directive = new ThemeSwitcherDirective();
    expect(directive).toBeTruthy();
  });
});
